# Database Configuration
POSTGRES_DB=impulseclub_db
POSTGRES_USER=impulse_admin
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# For Railway deployment, use this instead:
# DATABASE_URL=postgresql://user:password@host:port/database

# JWT Configuration
JWT_KEY=your_base64_encoded_secret_key_here_min_256_bits
JWT_ISSUER=ImpulseClubAPI
JWT_AUDIENCE=ImpulseClubUsers

# Application Port (Railway will override this)
PORT=5000